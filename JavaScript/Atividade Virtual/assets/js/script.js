/* Matheus Lima da Cruz*/

//Array que armazena Carga Horaria
const CH = []

//Função que calcula a carga horaria total
const chTotal = (x) => {
    return x.reduce((x,acc ) => x + acc ,0)
}

const ano_periodo = []

const tempoCurso = (x) => {

}

const mediaPonterada = (x,y) => {
    const mult = (x*y)
}
mediaPonterada([1,2,3,4,5],[2,2,2,2,2])

const listaDiciplinas = (x) => {
    
}